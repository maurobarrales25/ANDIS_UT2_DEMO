events{}
http{
    server{
        listen 80;

        location /ArtistAPI/{
            proxy_pass http://artist-api:8080/artist;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}